# AI Usage Log

Required by CS3219 Appendix 2: _"Maintain a log, `/ai/usage-log.md`, in the
repository with timestamps, prompts, and usage scenarios."_ This log is shared —
every team member appends to it, and so does every AI tool that changes files.

The consolidated AI Use Summary required as the README's last section is
generated from this log, so keep entries accurate rather than tidy. What may and
may not be delegated to an AI tool is defined in **§1 of the root `AGENTS.md`**
— that is the single copy; do not restate it here.

## How to add an entry

Append one row per working session to the table under [Entries](#entries),
newest at the bottom. That is the only table in this file.

| Column        | What goes in it                                                                                                      |
| ------------- | -------------------------------------------------------------------------------------------------------------------- |
| Date/Time     | ISO 8601 local, e.g. `2026-09-15T18:40`                                                                              |
| Member        | The human accountable. An agent fills this from `git config user.name`                                               |
| Tool / model  | e.g. `Claude Code (Opus 5)`, `Codex (GPT-5)`                                                                         |
| Mode          | `generate`, `refactor`, `debug`, `explain`, `docs`, or `test`                                                        |
| Scope         | Files or areas touched, and what changed                                                                             |
| Prompt        | The prompt as actually typed                                                                                         |
| Key responses | What the tool produced or concluded in reply — the decision it made, the approach it took, or what it declined to do |
| Human review  | What you checked and verified — `PENDING` until a human has                                                          |

**Prompts.** Record the prompt as actually typed, not a cleaned-up version — the
policy asks for "the exact prompts", and a reconstruction that reads better than
the original is itself a disclosure problem.

## File-header attribution

Every file an AI tool created, or whose lines it changed more than half of in
one session, carries this block at the top in that file's comment syntax:

```
// AI Assistance Disclosure:
// Tool: <product> (model: <model>), date: <YYYY-MM-DD>
// Scope: <what it generated or changed in this file>
// Author review: PENDING — <reviewer to complete>
```

Below that bar, mark the individual blocks inline instead:
`// AI-generated (edited by <name>).` When unsure which applies, mark inline —
over-disclosing is never the violation.

A human replaces `PENDING` with their name and what they actually checked.
Appendix 2 makes each member "fully responsible for understanding and validating
any AI-assisted code", and every member "remains accountable for understanding
the entire codebase". **Do not sign off on a file you have not read.** An agent
never writes a name into that field, and never removes an existing header.

## Entries

<!-- prettier-ignore -->
| Date/Time | Member | Tool / model | Mode | Scope | Prompt | Key responses | Human review |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-09-15T18:40 | twhjames | Claude Code (Opus 5) | generate | Root + per-service `AGENTS.md`/`CLAUDE.md` and this log — agent-instruction scaffolding, no application code | we will be using claude and codex, so we need both claude.md and agents.md - do we need to have these 2 in each service folder and root folder? what content goes to which? context for our product: Go for backend services, frontend language and framework TBC, OpenAPI for API contracts and SwaggerUI for API testing and communicating specs, Docker for containerisation, GitHub Actions for CI/CD, AWS for cloud deployment plus IaC (suggest one apt for us), and 5 developers with one owning each service plus a frontend developer. we also want proper unit, integration and end-to-end testing, no code smells, and to avoid control, global and temporal coupling and other anti-patterns. | Confirmed from the Claude Code docs that Claude Code reads only `CLAUDE.md` while Codex reads `AGENTS.md`, so both filenames are needed at the root and in each service folder. Proposed a single source of truth: `AGENTS.md` carries all content and every `CLAUDE.md` is a three-line `@AGENTS.md` import, so the two tools cannot drift. Root holds what is repo-wide — stack, folder ownership, the port and env-var allocation, service-independence and coupling rules, code quality, the three test tiers, API-contract and git conventions, and the Appendix 2 boundary. Each service file holds only its own bounded context, boundaries, package layout, local development and gotchas, and references the root rather than repeating it. Root files load at session start while a service pair loads on demand, so work on one service does not pull in the other four. Separately recommended spec-first OpenAPI with `oapi-codegen` and generated code committed, Terraform as the IaC tool once AWS work starts, and splitting container work by ownership — each service's Dockerfile in that service's own PR, the shared Compose baseline in its own. | Read all six AGENTS.md; confirmed the @AGENTS.md import works in Claude Code and that Codex reads AGENTS.md. Checked usage-log.md carries the timestamp, prompt, key-response and scenario fields Appendix 2 asks for. Verified no application code written as well. |
| 2026-09-17T18:49 | Nigeltzy | Claude Code (Opus 5) | generate | `frontend/` — ported the prototype (`index.html`, `app.js`, `styles.css`) to React + Vite + TypeScript: feature-sliced under `src/` — `features/home/`, `features/suppliers/` (SuppliersView, SupplierCard, SupplierDetail, SupplierForm, SupplierImage, `suppliersApi.ts`, `types.ts`), `components/` (Modal, Toast — shared only), `lib/config.ts` + `lib/http.ts`, `App.tsx`, `views.ts`, `main.tsx`, `src/styles.css` (prototype stylesheet moved, React-port selectors appended), `package.json`, `vite.config.ts`, `tsconfig*.json`, `Dockerfile` (added a Node build stage), `.dockerignore`, `frontend/.gitignore`, `frontend/.env.example`. Also recorded the stack decision and the new layout, owner and commands in `frontend/AGENTS.md`, the stack row in root `AGENTS.md` §2, and added `VITE_SUPPLIER_BASE_URL` to the root `.env.example`. `frontend/app.js` deleted. | can we work on our own branch and pull in those changes first, you and i will be in charge of the UI → (answering the agent's questions) frontend stack: React + Vite; language: TypeScript; record the decision in both `frontend/AGENTS.md` and root `AGENTS.md` §2, flagging the shared-file touch → Does it follow this format: src/ features/ ... components/ # shared UI used across features ... lib/ # shared setup (HTTP client, config) ... App.jsx main.jsx → it should be in features/home | Declined to pick the frontend stack itself, citing root §1 and `frontend/AGENTS.md` — presented four options without a recommendation and asked the folder owner to choose. Then declined to implement against the choice until it was written down, on the §1 rule that a decision stated in a prompt is not a record; wrote the record into both `AGENTS.md` files first. Ported the prototype behaviour 1:1 rather than redesigning: same views, filters, admin CRUD flow and stylesheet. Kept all transport in one hand-written client module per `frontend/AGENTS.md` so a generated OpenAPI client can replace it in one file; components take data and callbacks only. Initially scaffolded a flat `components/` + `api/` layout; restructured to the feature-sliced layout the owner then specified. When extracting `lib/http.ts`, deliberately left non-2xx error decoding in each feature's api module rather than centralising it, naming the `frontend/AGENTS.md` rule that whether the other services share supplier-service's `{"error": "..."}` envelope is their owners' interface decision. Replaced `window.confirm()` with an inline confirmation step. Renamed the Orders nav item to Errands to match the root file's domain vocabulary. Left state management, router, component library, CSS framework, linter and test runner explicitly unchosen and recorded them as still open. `npm run build` passes under `strict` TypeScript. | PENDING |
| 2026-09-17T19:03 | Nigeltzy | Claude Code (Opus 5) | generate | `frontend/` — restyled the UI to the owner's mockups and added the app shell. New `src/styles.css` design system (navy/orange tokens, cards, sidebar, mobile tab bar); `components/AppShell.tsx`, `components/icons.tsx`, `components/MockBadge.tsx`; `lib/mock.ts`; new features `auth/` (LoginPage + authApi), `errands/` (NewErrandView, MyErrandsView, errandsApi), `credits/` (CreditsView, creditsApi), `profile/` (ProfileView), each with its own `types.ts`; rewrote `App.tsx`, `views.ts`, `features/home/HomeView.tsx`; updated `features/suppliers/SuppliersView.tsx` for the new shell. Documented the mock layer in `frontend/AGENTS.md`. | I already made mockups previously can you update the look of the UI to be like this (three mockup images attached: login, request-an-errand, credits) → (answering the agent's questions) product name: Friend on Campus; scope: "Design the system + shell, then make stubs or stand-in-place for services that are not available yet. Aka mock those services, make a mock api endpoint or something according to what is expected" | Flagged before starting that the mockups say "Fetch on Campus" while the README and both `AGENTS.md` files say "Friend on Campus", and asked which is current rather than picking; the owner kept "Friend on Campus". Stated that a mock endpoint implies a payload shape and that API interfaces for user-, order- and credit-service belong to their owners under root §1, then built the mocks as frontend-local fixtures behind each feature's api module, with a file-level notice that the shapes are not a contract and must be replaced rather than reconciled when the real services land. Added a visible `<MockBadge />` on every fixture-backed screen so a graded demo cannot read invented figures as live data, and declined to invent a notifications feed (left visibly disabled). Kept the real supplier-service as the data source for the errand supplier picker, composing two services in one view. Flagged the campus delivery-location list and the 100-credit initial allocation as placeholders nobody owns. `npm run build` passes under `strict` TypeScript; Vite dev server compiles all modules. Not visually verified — no browser automation available and Docker is not installed, so supplier-service could not be run. | PENDING |
| 2026-09-17T19:09 | Nigeltzy | Claude Code (Opus 5) | refactor | `frontend/src/features/errands/` — replaced the placeholder four-value `ErrandStatus` with the seven-state set the team recorded (OPEN, ACCEPTED, PICKED_UP, DELIVERED, COMPLETED, CANCELLED, EXPIRED) plus a `TERMINAL_STATUSES` constant; updated `errandsApi.ts` fixtures and `MyErrandsView.tsx` labels/badges. Noted the exception in `frontend/AGENTS.md`. | The team already decided on the errand statuses. ALso I thought i told you to pull the ai/usage-log.md earlier so we could just work on it in our own branch (screenshot of the Order state row from the team's milestone report glossary attached) | Confirmed `ai/usage-log.md` was already merged into this branch and had been appended to since the branch was created; corrected a misleading earlier statement that it existed only on branches. Treated the milestone report's glossary as a recorded decision under root §1 — a written team artifact, as distinct from a decision asserted only in a prompt — and transcribed the state set rather than declining. Kept F3.8 transition rules out of the frontend entirely, noting in the type that order-service enforces them and the frontend only renders what it is given. Left the wire casing flagged as order-service's serialisation decision. The remainder of the Errand shape stays a frontend-local mock. `npm run build` passes. | PENDING |
| 2026-09-19T14:55 | Nigeltzy | Claude Code (Opus 5) | generate | `frontend/` — new `src/lib/tokens.ts` (AT/RT store) and `src/features/auth/session.ts` (attach + refresh-on-401 retry); rewrote `src/lib/config.ts` and `src/features/auth/authApi.ts`; edited `src/lib/http.ts`, `src/features/auth/LoginPage.tsx`, `src/App.tsx`, `frontend/.env.example`, `frontend/AGENTS.md`; root `.env.example` frontend block | (same session as the decision-record entry above) ... 5. Yes so far just keep it invented, unless another git branch already has the frontend auth paths written. | Implemented the client half of the recorded token lifecycle (D-011, D-014, D-015): tokens held in a closure created once in `App.tsx`, `Authorization: Bearer` attached by the shared transport, and a single-flight refresh-on-401 retry. Drove refresh off the gateway returning 401 rather than reading `exp` client-side, because reading `exp` means trusting an unverified token and would miss D-014's revocation cases where a token is unexpired but dead. Kept the auth endpoint paths and payloads explicitly marked as INVENTED — D-010..D-015 record the lifecycle, not the interface, which is user-service's under root §1 and spec-first under §8. Re-searched every ref first to confirm no branch already had them; none did. Declined to persist the tokens: where they live is still an open question in `ai/decisions.md`, so nothing touches `localStorage`, `sessionStorage` or cookies, and the visible cost (reload signs you out) is documented rather than hidden. Chose fixture vs gateway client once in `App.tsx` instead of threading a boolean into the auth functions (root §5, control coupling). Left `VITE_SUPPLIER_BASE_URL` in place as a marked transitional entry, because the gateway cannot forward yet and removing it would break the only working integration in the repo. Flagged that D-013 turns the admin-mode checkbox into a privilege escalation unless the gateway strips client-supplied claim headers, and did not touch `supplier-service` to fix it — that is its owner's folder. `npm run build` passes under strict TypeScript. Not visually verified. | PENDING |
| 2026-09-20T01:20 | Nigeltzy | Claude Code (Opus 5) | generate | `frontend/src/features/auth/` — new `validation.ts`, `fixtureOtp.ts`, `OtpStep.tsx` and two test files; rewrote `authApi.ts`, `LoginPage.tsx`, `types.ts`; touched `App.tsx`, `AppShell.tsx`, `ProfileView.tsx`, `styles.css`, `package.json`, `frontend/AGENTS.md` | Asked to build the OTP registration flow and fix the signup form fields, after the D1 backlog showed five High-priority OTP requirements with no UI at all | Implemented F1.1.1, F1.1.2.1, F1.1.2.3-F1.1.2.7, F1.1.3.1, F1.1.4.2, F1.2.1, F1.2.4 and the F1.4.1 profile fields. Requirement IDs were copied from the D1 milestone document, never constructed. Registration no longer returns a session: F1.1.2.7 makes the OTP step mandatory. Installed vitest and treated it as an open stack choice, because `frontend/AGENTS.md` listed a test runner as unchosen. **That was wrong** — the team had chosen vitest at the start; the file was stale. Corrected in a follow-up commit. 57 tests pass; `npm run build` clean | PENDING |
| 2026-09-21T00:15 | Nigeltzy | Claude Code (Opus 5) | refactor | `frontend/` — new `src/lib/jwt.ts`; rewrote the gateway half of `src/features/auth/authApi.ts`; edited `src/features/auth/session.ts`, `src/features/auth/types.ts`, `src/lib/tokens.ts`, `src/App.tsx`, `src/features/profile/ProfileView.tsx`. No backend folder touched. | Do you know the current state of the repo, as well as what I'm in charge of? If so, then move on to the fact that my teammate in charge of user-service told me that they pushed their contributions of their service to the repo, so maybe we can work off that next → I don't understand, explain the collisions, where is the collision (on frontend or user-service or what), etc → No, we need to update the frontend and api gateway based on the user-service endpoints, because our side is outdated → (answering the agent's questions) forward Authorization on `/api/users/*` only; flag the profile gap for Zi Yang and render what exists; asked whose job the OTP flow is | Read `origin/feat/user-service` and reconciled the frontend against its committed `user-service/api/openapi.yaml`, treating that spec as the record under root §1 and naming it as the source for every shape transcribed. Fixed four concrete mismatches: logout now sends the `refreshToken` its `LogoutRequest` marks required (it previously sent none, so nothing was revoked); refresh now stores the whole rotated pair, because user-service rotates RTs and answers `ErrSessionCompromised` to a replayed one — `TokenStore.setAccessToken` was removed as its documented assumption was disproven; login builds its session from the access token's `sub`/`role` plus `GET /api/v1/users/{uid}`, since `AuthResponse` carries no user object; `email` and `contact` became optional. Added `lib/jwt.ts` to read display claims from an unverified token, documented at length as NOT authentication — the gateway's RS256 check against the JWKS remains the only thing that decides. Declined to invent the missing pieces. The three OTP calls now refuse before sending: user-service's `register` creates the account and returns an empty 201, so calling it would have produced a real orphan account behind a 'registration failed' message. Stated that F1.1.2.3-F1.1.2.7 are user-service's to build because the code, its expiry and the resend limits are server-side rules, and that enforcing them in the browser would make them bypassable. Reported that `UserResponse` has no email and that contact exists nowhere in user-service, so F1.4.1 cannot be met from the current contract, and left that as an interface change for its owner rather than filling it from what the UI guessed. Also identified, without acting on them: the gateway's `/api/users` route strips its prefix to `/{uid}` while user-service serves `/api/v1/users/{uid}`; user-service's `RequireJWT` reads `Authorization`, which D-022 has the gateway delete; and `compose.yaml` conflicts between this branch and `feat/user-service`. `npm run build` clean under strict TypeScript, 57 vitest tests pass. Not run against a live user-service — no Docker. | PENDING |
| 2026-09-21T23:20 | Nigeltzy | Claude Code (Opus 5) | edit | `frontend/src/features/auth/authApi.ts`, `frontend/src/features/auth/types.ts`, `frontend/src/features/profile/ProfileView.tsx` (comments only), new `frontend/src/features/auth/gatewaySession.test.ts`, and this log. No other folder touched. | Zi Yang pushed 14 commits to `origin/feat/user-service` (72fe5a5); asked what the frontend and gateway need in response, then to make the frontend change | Reviewed the push before changing anything. **Gateway needs no change** — verified that `routes.go` still mounts `/api/v1/users/*` and `/.well-known/jwks.json` despite the package restructure, and that `RequireJWT` still reads the bearer off `Authorization`, so `AddPrefix`, `JWKS_URL` and `NewRetainingToken` all remain correct. **Frontend:** their `UserResponse` gained `email` and `phone_num` and a new `RestrictedUserResponse` carries both for non-admin callers, so `sessionFromTokens` now populates `Session.email` and `Session.contact` from the profile call it already made. Transcribed from their committed `user-service/api/openapi.yaml` at 72fe5a5 — no field invented here, and `phone_num` is renamed to the backlog's `contact` at this one boundary. **This closes the F1.4.1 gap** that three comment blocks in this folder still described as unfillable; all three corrected rather than left to mislead. Blank values map to undefined so views test one thing. Noted in a comment that the `profile.account_role` fallback is now dead for non-admins, since `RestrictedUserResponse` omits the field — the token claim is preferred anyway. Five tests added covering the mapping, the blank case, the role source and a failed profile call; they stub `fetch` and assert this folder's mapping only, not user-service's behaviour. Also checked their password policy (8–128, upper/lower/digit) and `@u.nus.edu` rule against `validation.ts` and found them already identical — no change made. `npm run build` clean under strict TypeScript, 62 vitest tests pass. Not run against a live user-service — no Docker. | PENDING |
