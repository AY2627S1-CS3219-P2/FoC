# AI Usage Log

Required by CS3219 Appendix 2: _"Maintain a log, `/ai/usage-log.md`, in the
repository with timestamps, prompts, and usage scenarios."_ This log is shared —
every team member appends to it, and so does every AI tool that changes files.

The consolidated AI Use Summary required as the README's last section is
generated from this log, so keep entries accurate rather than tidy. What may and
may not be delegated to an AI tool is defined in **§1 of the root `AGENTS.md`**
— that is the single copy; do not restate it here.

## How to add an entry

Append one row per working session to the table under [Entries](#entries),
newest at the bottom. That is the only table in this file.

| Column        | What goes in it                                                                                                      |
| ------------- | -------------------------------------------------------------------------------------------------------------------- |
| Date/Time     | ISO 8601 local, e.g. `2026-09-15T18:40`                                                                              |
| Member        | The human accountable. An agent fills this from `git config user.name`                                               |
| Tool / model  | e.g. `Claude Code (Opus 5)`, `Codex (GPT-5)`                                                                         |
| Mode          | `generate`, `refactor`, `debug`, `explain`, `docs`, or `test`                                                        |
| Scope         | Files or areas touched, and what changed                                                                             |
| Prompt        | The prompt as actually typed                                                                                         |
| Key responses | What the tool produced or concluded in reply — the decision it made, the approach it took, or what it declined to do |
| Human review  | What you checked and verified — `PENDING` until a human has                                                          |

**Prompts.** Record the prompt as actually typed, not a cleaned-up version — the
policy asks for "the exact prompts", and a reconstruction that reads better than
the original is itself a disclosure problem.

## File-header attribution

Every file an AI tool created, or whose lines it changed more than half of in
one session, carries this block at the top in that file's comment syntax:

```
// AI Assistance Disclosure:
// Tool: <product> (model: <model>), date: <YYYY-MM-DD>
// Scope: <what it generated or changed in this file>
// Author review: PENDING — <reviewer to complete>
```

Below that bar, mark the individual blocks inline instead:
`// AI-generated (edited by <name>).` When unsure which applies, mark inline —
over-disclosing is never the violation.

A human replaces `PENDING` with their name and what they actually checked.
Appendix 2 makes each member "fully responsible for understanding and validating
any AI-assisted code", and every member "remains accountable for understanding
the entire codebase". **Do not sign off on a file you have not read.** An agent
never writes a name into that field, and never removes an existing header.

## Entries

<!-- prettier-ignore -->
| Date/Time | Member | Tool / model | Mode | Scope | Prompt | Key responses | Human review |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-09-15T18:40 | twhjames | Claude Code (Opus 5) | generate | Root + per-service `AGENTS.md`/`CLAUDE.md` and this log — agent-instruction scaffolding, no application code | we will be using claude and codex, so we need both claude.md and agents.md - do we need to have these 2 in each service folder and root folder? what content goes to which? context for our product: Go for backend services, frontend language and framework TBC, OpenAPI for API contracts and SwaggerUI for API testing and communicating specs, Docker for containerisation, GitHub Actions for CI/CD, AWS for cloud deployment plus IaC (suggest one apt for us), and 5 developers with one owning each service plus a frontend developer. we also want proper unit, integration and end-to-end testing, no code smells, and to avoid control, global and temporal coupling and other anti-patterns. | Confirmed from the Claude Code docs that Claude Code reads only `CLAUDE.md` while Codex reads `AGENTS.md`, so both filenames are needed at the root and in each service folder. Proposed a single source of truth: `AGENTS.md` carries all content and every `CLAUDE.md` is a three-line `@AGENTS.md` import, so the two tools cannot drift. Root holds what is repo-wide — stack, folder ownership, the port and env-var allocation, service-independence and coupling rules, code quality, the three test tiers, API-contract and git conventions, and the Appendix 2 boundary. Each service file holds only its own bounded context, boundaries, package layout, local development and gotchas, and references the root rather than repeating it. Root files load at session start while a service pair loads on demand, so work on one service does not pull in the other four. Separately recommended spec-first OpenAPI with `oapi-codegen` and generated code committed, Terraform as the IaC tool once AWS work starts, and splitting container work by ownership — each service's Dockerfile in that service's own PR, the shared Compose baseline in its own. | Read all six AGENTS.md; confirmed the @AGENTS.md import works in Claude Code and that Codex reads AGENTS.md. Checked usage-log.md carries the timestamp, prompt, key-response and scenario fields Appendix 2 asks for. Verified no application code written as well. |
| 2026-09-19T14:50 | Nigeltzy | Claude Code (Opus 5) | generate | New `api-gateway/` — `go.mod`, `cmd/api/main.go`, `internal/config` (+ empty test), empty `internal/auth|revocation|proxy|httpapi` `doc.go` files, `api/README.md`, `Dockerfile`, `.env.example`, `AGENTS.md`, `CLAUDE.md`, `README.md`. Root `AGENTS.md` §3 layout and port table; root `.env.example` gateway block | ... 1. Go ahead and commit ... 4. Just use whatever ports are default and work for the time being | Kept strictly to scaffolding as root §1 defines it — directories, module, wiring, config plumbing, empty test file — and wrote no token verification, no Redis client and no proxying. Each empty package's `doc.go` names what blocks it rather than leaving it silent. `config.Load` returns an error listing every missing variable instead of falling back to defaults, so a missing value fails at boot rather than silently addressing the wrong thing. Added no dependency: the module requires nothing beyond the standard library, and `chi/v5` (D-002) was deliberately left out until the first real route exists. Ports 8080/6379 were taken from the owner's instruction and recorded as **provisional** (D-018) rather than presented as settled. Flagged both shared-file touches (root `AGENTS.md`, root `.env.example`) per root §3. **NOT COMPILED OR VETTED** — no Go toolchain is installed on this machine, so `go build`, `go vet` and `gofmt` have not been run against any of it. | Checked all relevant files and ensured that the layouts match the tech stack the team has in mind. |
| 2026-09-19T15:10 | Nigeltzy | Claude Code (Opus 5) | generate | Root `compose.yaml` — extended the PR #1 baseline with `redis` and `api-gateway`; root `AGENTS.md` §10 command note | Draft the compose.yaml | Built on the existing Compose baseline from `origin/feat/supplier-service` rather than inventing one, keeping its one-database-per-service topology (D-003) and its service names. Enforced D-010's trust zones through the presence or absence of a `ports:` key, and wrote that rule into the file's header so it is not accidentally undone. Gave Redis no published port and turned on append-only persistence, because a restart would otherwise empty the `jti` blocklist and silently un-revoke every token logged out before it. Left `supplier-service`'s published 8082 in place but marked it TRANSITIONAL and said plainly that the diagram's zones are not yet enforced for it — removing it now would break the frontend, since `internal/proxy` is an empty scaffold. Omitted `user-service`, `order-service` and `credit-service`: they have Dockerfiles but no source, so listing them would break `docker compose up`. Took `JWT_SECRET` from the gitignored `.env` with Compose's required-variable syntax rather than hardcoding any value. **NOT VALIDATED** — Docker is not installed on this machine, so `docker compose config` and `docker compose up` have not been run. The file parses as YAML, which is all that was checked. | Checked all relevant output and checked on Docker on my own device |
| 2026-09-19T19:45 | Nigeltzy | Claude Code (Opus 5) | docs | `ai/decisions.md` — marked D-011 and D-017 Superseded and appended D-019 (role values `STUDENT`/`ADMIN`), D-020 (`user-service` sole Redis writer, gateway reader) and D-021 (requester/courier is semantics, not a role axis); removed the two Open rows those close. `ai/decisions/D-010-api-gateway-auth.md` — updated the trust-zone, role-claim and "does not settle" passages to match | Reviewed a `user-service/AGENTS.md` sent by Zi Yang against the decision record; named four conflicts; Nigeltzy resolved three in favour of that document and deferred the fourth (JWT_SECRET vs RS256 keypair in the root `.env.example`) to the team | Transcription only — every Rationale cell left blank, no row invented, no schema or interface authored. Flagged that D-020 needs a human-written carve-out against root `AGENTS.md` §4.1 and D-003, since two services now touch one Redis | Used for comparison and intepretting files, decided on architecture by myself as discussed with team |
| 2026-09-19T19:55 | Nigeltzy | Claude Code (Opus 5) | edit | `api-gateway/internal/revocation/doc.go`, `api-gateway/AGENTS.md`, `api-gateway/.env.example` — doc comments and config comments only, no behaviour | Asked whether the endpoints in Zi Yang's user-service AGENTS.md required changes here | Answered no: that file is on no branch, so root §1 does not count it as recorded, and its paths are user-service-facing while the frontend addresses the gateway. Did change what D-020 made factually wrong — three passages still said D-017 gave Redis to the gateway exclusively and that user-service had to ask the gateway to revoke | Used for interpreting and formatting |
| 2026-09-19T20:20 | Nigeltzy | Claude Code (Opus 5) | edit | `api-gateway/internal/config/config.go` (stale D-017 comment; `sort.Strings` on the missing-vars list), `compose.yaml` (stale D-017 comment), new root `.dockerignore`, plus a D-003 over-citation corrected in `ai/decisions.md`, `ai/decisions/D-010-api-gateway-auth.md` and `api-gateway/AGENTS.md` | Walked the human through `config.go`, `main.go` and `compose.yaml` before sign-off, then was asked to fix what the walkthrough found | Six findings; four fixed here. The build context for both root-context images was the whole repo with no `.dockerignore`, so every image build shipped ~69 MB of `frontend/node_modules`. `config.go`'s missing-variable error was non-deterministic because Go randomises map iteration. D-003 was cited as conflicting with D-020 but covers PostgreSQL only, not Redis | Used for interpeting code base |
| 2026-09-19T21:20 | Nigeltzy | Claude Code (Opus 5) | edit | Root `AGENTS.md` (port/env table) and root `.env.example` (revocation-store comment) — both SHARED files, flagged per §3 | Drafting the PR description surfaced two more places still describing D-017 | Both said the gateway owns Redis exclusively and that no other service gets the connection string. D-020 makes user-service the sole writer, so both hold it. Correcting the branch's own text rather than leaving the PR shipping two contradictory statements | Used to intepret and comapare the different commits |
| 2026-09-19T21:55 | Nigeltzy | Claude Code (Opus 5) | docs | `ai/decisions.md` — new rows D-023 (RS256/JWKS), D-024 (gateway does not connect to Redis), D-025 (prototype-phase risk acceptance); D-013 marked Amended, D-020 marked Superseded; three Open rows struck. `ai/decisions/D-010-api-gateway-auth.md` — trust zones, routing step, revocation and "does not settle" sections brought in line | Nigeltzy and Zi Yang settled four open points together and asked for them written down | Transcription of their decisions. One clarifying question was put back to them first: whether the gateway checks revocation per request via user-service, or not at all. They chose not at all, which is what D-024/D-025 record. Rationale cells left blank. Flagged separately that api-gateway config, both .env.example files and compose.yaml still contradict D-023 and D-024 | Used to format the docs based on our team's internal discussions |
| 2026-09-19T22:20 | Nigeltzy | Claude Code (Opus 5) | generate | `api-gateway/` built out from scaffold to working: new `internal/auth/auth.go` (RS256 + JWKS), `internal/proxy/{proxy,context}.go`, `internal/httpapi/{router,middleware}.go`, rewritten `cmd/api/main.go` and `internal/config/config.go`; new tests `internal/proxy/proxy_test.go` and `internal/httpapi/router_test.go`; deleted `internal/revocation/` and the four placeholder `doc.go` files. Config updated in `api-gateway/.env.example`, root `.env.example`, `compose.yaml` (both SHARED, flagged per §3) and `api-gateway/AGENTS.md` | Nigeltzy took ownership of the gateway and asked for a barebones version that others can extend. Two interface questions were put back to them first: the public route surface, and whether to follow D-005 spec-first | They chose /auth/* plus a prefix proxy (D-027) and code-first (D-028). Implemented to that. Added two dependencies with reasons: chi/v5 (D-002) and golang-jwt/jwt/v5 for RS256; JWKS parsing left to the stdlib. Verified: gofmt clean, go vet 0, go test 0 (14 tests), docker compose config 0, and a live process answering 200/401/502/404 with the /auth/login to /api/v1/users/login rewrite confirmed in its log | Built out scaffolding for new service that the team decided to work on as an intermediary |
| 2026-09-19T23:05 | Nigeltzy | Claude Code (Opus 5) | docs | New `ai/decisions/architecture.md` — the team's PNG redrawn as five Mermaid diagrams and updated to D-019..D-028; links added from `ai/decisions.md` and `ai/decisions/D-010-api-gateway-auth.md` | Asked to convert the PNG architecture diagram to Mermaid and bring it up to date with what the team decided and what is actually built | Transcription of recorded rows only — no architecture authored, no Rationale offered. Two places where the PNG is now wrong are called out in the text rather than silently redrawn: Redis was attached to the gateway (D-024 removed it) and logout was shown killing the access token (D-025a says it does not). Diagrams validated with mermaid 11's own parser under jsdom; 2 of 5 failed on first attempt because semicolons terminate statements in sequence diagrams, fixed, all 5 now parse. The PNG is kept, not deleted | Used to help convert the team's handdrawn diagram into mermaid for easier updating in the repo for the future |
| 2026-09-20T01:25 | Nigeltzy | Claude Code (Opus 5) | docs | `ai/decisions.md` — new row D-029 recording vitest as the frontend test runner | Recording the frontend test runner while building the OTP flow on `feat/frontend-ui` | **First written as though the choice were made today; corrected the same session.** vitest was the team's choice from the start — `frontend/AGENTS.md` was stale in listing a test runner as unchosen, and that stale text is what the agent acted on. The row is a transcription, and its Date cell is left for a human to fix. The row lands on this branch because `ai/decisions.md` exists only here; the code is on `feat/frontend-ui` | Used to format the team's update for the documentation |
| 2026-09-21T00:40 | Nigeltzy | Claude Code (Opus 5) | refactor | `api-gateway/` — `internal/proxy/proxy.go` (behaviour split, new `NewRetainingToken`), `internal/httpapi/router.go` (service route table), plus four tests in `internal/httpapi/router_test.go` and `internal/proxy/proxy_test.go`. No other folder touched. | No, we need to update the frontend and api gateway based on the user-service endpoints, because our side is outdated → (answering the agent's questions) forward Authorization on `/api/users/*` only | Reconciled the gateway's `/api/users` route with user-service's committed `api/openapi.yaml`, which landed on `origin/feat/user-service`. Two changes. The route now carries `AddPrefix: /api/v1/users`, where that service actually mounts its profile routes; it previously stripped the prefix to `/{uid}` and would have 404'd on every authenticated user request. And the bearer token is now retained on that one route, because user-service's own `RequireJWT` middleware authenticates on the Authorization header and its spec marks those paths `security: BearerAuth: []`. The `passthrough bool` on Proxy was split into two independent fields — whether the callee keeps the token, and whether the gateway has a verified identity to assert — since the old flag only worked while the two cases differed in both respects at once. Both remain set by named constructors rather than passed in (root §5). **D-022's strip is untouched and still unconditional**: client-supplied `X-User-Id`/`X-User-Role` are deleted before anything is injected, on every route, and two of the new tests assert exactly that under a caller sending `X-User-Role: ADMIN`. A third asserts supplier-, order- and credit-service still never receive a bearer token. **Flagged, not acted on:** this is a deliberate exception to D-022's 'downstream services do not parse JWTs' for one service, and it needs a decision row that a human writes (root §1). Also left alone with a comment: `/api/suppliers` has the same prefix bug, since supplier-service mounts at `/suppliers/{id}` — that is its owner's contract to confirm, not one to infer (root §3). `go build`, `go vet` and `go test ./...` all pass; `gofmt` is clean once line endings are normalised (`core.autocrlf` flags every file in the repo, touched or not). Not run against a live user-service — no Docker. | Ensured that the AI did not make any team decisions, used to refactor and improve code |
| 2026-09-21T23:45 | Nigeltzy | Claude Code (Opus 5) | docs | `ai/decisions.md` — one row appended to the **Open** table (no row added to the decision table itself), plus the HTML comment marking it as AI-added. Nothing else on this branch touched. | A PR review comment on D-013 asked whether the row should say gRPC rather than synchronous REST, "since we will be using gRPC for internal", or whether a new entry would be added. Nigeltzy asked for the Open row | Did not edit D-013 and did not add a decision row. Under root §1 an assertion in a review comment is not a record, and `grpc` appears in no row and in no code on any branch (the only match anywhere is a transitive `protobuf` line in user-service's `go.sum`), so there was nothing to transcribe. Recorded the gap instead, which is what the Open table exists for. Named, without resolving them, the rows a gRPC decision would have to settle: D-013 covers the gateway→service hop specifically, and if gRPC covers that hop then D-022 goes with it — `X-User-Id`/`X-User-Role` are HTTP headers, and the strip-and-inject in `internal/proxy/proxy.go` that every service's access control rests on would have to be respecified as metadata. Also noted D-005's OpenAPI pipeline and that the browser hop stays HTTP either way. Separately confirmed for the same question that the Open table already carries the broker technology as unanswered, so Kafka is open too. No Rationale cell filled. | Used to update documentation |
| 2026-09-22T13:13 | Nigeltzy | Claude Code (Opus 5) | docs, generate | **`ai/decisions.md`** — new rows **D-030** (the gateway forwards the bearer to `user-service` only, D-022's one exception), **D-031** (service-to-service over gRPC) and **D-032** (broker is Kafka); **Amended by D-030** added to D-022's Status; the Open rows on transport and broker **narrowed, not struck**; both transcription comments extended. **`api-gateway/internal/httpapi/router.go`** — `/api/suppliers` now carries supplier-service's `/suppliers` prefix (new `supplierServiceAPIPrefix`), and an **interim permissive CORS policy** (`interimCORS`) added ahead of the auth middleware. **`api-gateway/internal/httpapi/router_test.go`** — four new tests (two supplier prefix, two CORS); `TestServiceRouteForwardsVerifiedIdentity`'s path assertion updated `/42` → `/suppliers/42`. **`api-gateway/go.mod` / `go.sum`** — added `github.com/go-chi/cors v1.2.2`. **`api-gateway/internal/proxy/proxy.go`** — comment only. **`api-gateway/AGENTS.md`** — dependency list and a CORS entry under "Still open". Then, on instruction, committed the above as three commits and merged `main` into both `feat/api-gateway-auth-proxy` and `feat/frontend-ui`. The gateway merge conflicted only on **`compose.yaml`** (a SHARED file, flagged per root §3): resolved in favour of this branch, which is a superset of main's half, and three stale comments in it rewritten — the "PR #1 not yet merged" note, the TRANSITIONAL note on the published 8082, and the redis block, which now names the persistence difference with `feat/user-service`'s half. The frontend merge conflicted only on root `AGENTS.md`, one table row, resolved in main's favour (it names vitest, which is D-029) |  (after being given `handoff210926.txt`) start with 5.4, but make sure you look through all the updated pushes on other branches by my teammates as well to see what needs to be done → Do we know if the frontend and api gateway works with user-service and supplier-service yet, or do we have to merge or what not → use the same permissive CORS as supplier-service, marked interim. then check the newly pushed changes from Zi Yang 9 minutes ago to see if its relevant | **Transcription and implementation of records, plus one gap closed on instruction.** D-030 from Nigeltzy's handoff §5.4 and `04475c4`; D-031/D-032 from root `AGENTS.md` on `main` (`dbeb950`, PR #1), whose own log row records them as applied verbatim from the team's `agents_md_changes.md`; the supplier prefix from `supplier-service/internal/httpapi/router.go` on `main`. **CORS was added on Nigeltzy's explicit instruction** to mirror supplier-service's permissive policy and mark it interim — the gateway had none, and with nginx serving static only, every authenticated browser call was failing its preflight. Three forced deviations from supplier-service's copy are documented at the constant: `Authorization` allowed (the browser sends it on every proxied route), `PATCH` allowed (user-service's `/status`), and `X-User-Role` **not** allowed (the gateway strips it per D-022; advertising it would suggest a client may assert its own role). Which origins a public edge accepts has no row in `ai/decisions.md` and is flagged as open, not decided. **Every Rationale cell left blank** (root §1). Reviewed Zi Yang's `253fc92` and reported findings without acting on another owner's folder. Declined to resolve: D-031's gRPC scope, the D-024 vs `user-service/AGENTS.md` Redis conflict, F1.7.2 vs D-025a. Ran no git command beyond `fetch`. Verified: `go build`, `go vet` clean, `go test ./... -count=1` green, 25 tests; the preflight response was probed directly (200, `Access-Control-Allow-Origin: *`, `Allow-Headers: Authorization`). The seven `gofmt -l` hits are the `core.autocrlf` artefact, checked against index content. Re-verified after both merges: gateway 25 tests green, `supplier-service` (newly present from main) builds and its tests pass, `docker compose config` validates, and on `feat/frontend-ui` `npm run build` is clean under strict TS with 62 vitest tests passing — `app.js`/`styles.css` did not return and `index.html` is still the Vite entry. **Nothing pushed, no PR opened** | Used to intepret, format documentation as well as to generate all the boilercode necessary as well as integration for multiple incoming services from other team members  |
| 2026-09-21T21:54 | GCheeYang | Claude Code (Sonnet 5) | docs | Root `AGENTS.md` only, no code — applied the same four human-authored edits from the team's `agents_md_changes.md` to the supplier PR branch: the "Overall shape" line; stack table (gRPC, Kafka, `api-gateway` code-first exception, Frontend row corrected from TBC); §4 rule 3 (OpenAPI client to gRPC); §8 scope note and reference link. Merged `origin/main` into the branch first so the file exists here | can you add the changes to the PR i created — then, clarifying which PR: the feat(supplier service) PR | Merged main (clean, no conflicts), then applied the four edits verbatim with exact-match checks; the result is identical to the same edits on `feat/api-gateway-auth-proxy`. Made no decision of its own. Does not cover this PR's earlier supplier-service and frontend work, which still needs its own log row. | PENDING |
| 2026-09-22T14:05 | Nigeltzy | Claude Code (Opus 5) | docs | **`ai/decisions/architecture.md`** — corrected two errors in the diagrams this tool wrote on 2026-09-19 (the refresh flow showed `user-service` reading the Redis blocklist; it reads `account_status`/`tokens_valid_after` from PostgreSQL, and its Redis interface is `Set`-only. The lingering-token arrow in §5 pointed at `user-service`; a `/api/suppliers/1` request is proxied to `supplier-service`). Added **§6, "Who answers 'is this token still good?'"** — the Job 1 / Job 2 split, the three described models against the one that is built, a table showing both Redis keys have no reader, and the F1.7.2 vs D-025a question handed back. Refreshed §7's state table. Sections renumbered 6→7, 7→8 | is there anything wrong with the flow as built today? if not, then we take that as the current way forward → yes do both, then update me on the remaining followups, what works and what has not been implemented yet, then next steps to test the system | **Corrections to its own earlier output, plus explanation of existing code.** Every claim re-derived from source: `git grep` over all of `user-service` found exactly three Redis call sites (`ParseURL`, `NewClient`, and the `Set`-only `redisSetter`), `RefreshService.activeAccount` was read directly, and the gateway's absence of a Redis client was confirmed across `go.mod`, `internal/config`, `.env.example` and `compose.yaml`. **No architecture authored and no Rationale offered.** §6 states which models are recorded, which is built, and what the call graph does; it does not recommend one. The F1.7.2 / D-025a contradiction is named and handed to the team per root §1 | Used to update and format documentation |
| 2026-09-22T16:35 | Nigeltzy | Claude Code (Opus 5) | debug | **`api-gateway/internal/proxy/proxy.go`** — new `ModifyResponse` on the reverse proxy, deleting every `Access-Control-*` header a downstream service sets before the response reaches the browser. **`api-gateway/internal/httpapi/router_test.go`** — `stubService` now sets its own `Access-Control-Allow-Origin`, as supplier-service really does, plus `TestDownstreamCORSHeadersDoNotReachTheBrowser` | by the way it says this on new errand page: Could not load suppliers: Could not reach the service at http://localhost:8080 Check that supplier-service is running. | **A real bug, found by the user in a browser and invisible to the test suite.** supplier-service runs its own permissive `cors.Handler` from when the browser addressed it directly; `httputil.ReverseProxy` copies response headers through, so the browser received `Access-Control-Allow-Origin` **twice** and refused the response — 'the header contains multiple values, but only one is allowed'. `fetch()` then rejects, which `lib/http.ts` surfaces as `NetworkError` and the UI reports as the service being unreachable. Diagnosed by curling the gateway with an `Origin` header and counting the response headers (2), not by reading. The gateway is the public edge and owns the CORS policy alone, so a callee's headers are discarded there rather than asking another owner to change their file. **The regression test was verified to fail without the fix** — neutered `ModifyResponse`, confirmed the test reports 2, restored. `curl` cannot catch this class of bug at all, which is worth remembering: the 12/12 smoke run passed while the browser was broken. 26 tests green | Used to debug and fix issues with the test |
| 2026-09-22T17:30 | Nigeltzy | Claude Code (Opus 5) | generate | **`ai/decisions.md`** — new row **D-033** plus **`ai/decisions/D-033-browser-token-storage.md`**, and the Open row "Where the UI keeps the AT and RT" struck. **`api-gateway/internal/proxy/session_cookie.go`** — new: cookie attributes and the JSON body translation both ways. **`proxy.go`** — `NewSessionIssuing`/`NewSessionRotating`/`NewSessionEnding`, cookie→body in `Rewrite`, body→cookie in `ModifyResponse`. **`httpapi/router.go`** — `interimCORS` **deleted**, one constructor per auth route. **`internal/config`** — `REFRESH_TOKEN_TTL`. **`go.mod`** — `go-chi/cors` removed. Tests, `.env.example` and `api-gateway/AGENTS.md` updated | Why is login missing? Fix that. Then afterwards reference this screenshot, if everything said in it is valid, go ahead with the changes (screenshot: decisions 10-15, same-origin proxy) | **Transcription of Nigeltzy's fifteen numbered decisions, then implementation of them.** He made the calls across three messages after taking the options to the team; this tool wrote none of them and offered no Rationale — that cell is blank as everywhere else. One substantive point was contributed and confirmed by him before building: **login was missing from the routes that touch the cookie.** Checked against the committed `user-service/api/openapi.yaml` — `/register` returns 201 with no body, but `/login` returns `AuthResponse` carrying the refresh token, so it is where the cookie is first set. Earlier in the same session this tool wrongly said D-011 and D-015 would need superseding; re-read both and corrected — neither describes where the UI keeps the token, so D-033 is an addition, not a supersede. Item 8 (scoping `revokeCompromisedSessions` to the token family) was **not implemented — it is `user-service` code and Zi Yang's** (root §3); the schema finding was written into D-033 for him instead. `.env.example` and `compose.yaml` are shared files and the `REFRESH_TOKEN_TTL` addition is flagged. Verified: `go build`, `go vet` clean, `go test ./... -count=1` green, **30 tests** | Used to implement changes discussed by the team |
| 2026-09-22T17:20 | Nigeltzy | Claude Code (Opus 5) | generate | **The same-origin half of D-033 for Compose.** `api-gateway/internal/httpapi/static.go` — new: serves the built SPA with a client-side-route fallback to `index.html`. `internal/config` — `STATIC_DIR`, the one optional variable. `internal/httpapi/router.go` — registers it as the catch-all, LAST. `api-gateway/Dockerfile` — a node build stage; the bundle is baked in at `/app/web`. `compose.yaml` (**SHARED**, flagged per root §3) — the `frontend` service is **removed** and the gateway is published as `3001:8080`. `api-gateway/.env.example` — `STATIC_DIR`. Four new tests | (continuing) go ahead with the changes | Implements D-033 decision 12, "the gateway serves the built frontend from /". `frontend/Dockerfile` and `frontend/nginx.conf` are **kept, not deleted**, because decision 12 says to bring nginx back only if this proves unworkable and to flag that rather than doing it silently — deleting them would make that harder to reverse. Two tests exist specifically for the failure modes: that the catch-all does not shadow the API (an unauthenticated `/api/...` must still be 401, not a 200 page), and that an empty `STATIC_DIR` leaves the default 404, which is what `go run ./cmd/api` needs since Vite serves the app there. Verified: `go build`, `go vet` clean, `go test ./... -count=1` green, **33 tests**; `docker compose config` validates | Used to test an idea that I have, so it generated a different version of the client-side handling for me to see if it can work |
| 2026-09-23T10:05 | Nigeltzy | Claude Code (Opus 5) | debug | **`compose.yaml`** (**SHARED**, flagged per root §3) — removed `supplier-service`'s published `8082:8082`, which closes **D-025b**, and deleted a duplicated `# ---- Browser client` comment line left by an earlier merge resolution | show me the message to show to Zi Yang and confirm that it is still a relevant message after all of the PRs and branches present | Found while re-verifying the draft message against the current branches, in this tool's own open PR rather than a teammate's. The port's comment said to delete it "in the same change that points the frontend at the gateway (D-025b)" — that change shipped as PR #6, so the line was stale and the port was still exposing `supplier-service` to the host. With it gone, every service except the gateway has no `ports:` key and D-010's trust zones hold for the first time. Verified: `docker compose config` validates | Used to debug and solve a problem with outdated code, then used to generate update report / summary |
| 2026-09-26T12:35 | Nigeltzy | Claude Code (Opus 5.5) | debug, test, docs | Addressing twhjames's PR #4 review (2026-09-25). **Comments:** applied 133 of the reviewer's 135 inline suggestions verbatim across 20 files (the other two described the frontend stage and `STATIC_DIR`, which were removed instead). **`api-gateway/internal/auth/auth.go`** — `jwt.WithExpirationRequired()`; the JWKS throttle now records failed attempts too, and a `fetchMu` serialises refetches; the fetch is detached from the caller's cancellation; `ErrKeysUnavailable` now carries its cause; a token with no `kid` maps to `ErrMalformed`. **New `internal/auth/auth_test.go`.** **`internal/proxy/proxy.go`** — logout expires the refresh cookie whatever user-service answers (including 502), and refresh expires it on 401 but not on 5xx. **New `internal/proxy/session_test.go`.** **`internal/httpapi/router.go`** — unknown `/api` and `/auth` paths get a JSON 404 instead of the SPA; `middleware.RealIP` replaced by `middleware.Logger`. **`middleware.go`** — logs the cause of a 503. One new test in `router_test.go`; `<name>` in eight inline markers set to `PENDING`. **`api-gateway/Dockerfile`** — frontend stage and `COPY … ./web` removed (to move to PR #6). **`compose.yaml`**, root **`.env.example`**, root **`AGENTS.md`** (all **SHARED**, flagged per root §3) — `STATIC_DIR` removed, `GATEWAY_PORT` removed, §10 paragraph replaced with the reviewer's suggested text. **`.dockerignore`** — `**/` patterns (reviewer's suggestion). **`api-gateway/AGENTS.md`**, **`api-gateway/.env.example`** — stale facts corrected. **`ai/decisions.md`** — the two HTML comments narrating edits deleted, nothing else. **`ai/decisions/architecture.md`** — `/42` → `/suppliers/42`. **Later, same session:** after Nigeltzy rewrote D-013 (gRPC, decided but not built) and removed the `0.0.0.0:8082` claims from D-022/D-025 himself, those two changes were propagated to `ai/decisions/D-010-api-gateway-auth.md` (§2 step 2, the "No direct route" leg), `ai/decisions/architecture.md` (the three gateway→service edge labels, the LEAK edge, the §8 D-022 bullet) and `api-gateway/AGENTS.md` (the REST line). Transcription of his wording only; nothing added beyond what the code shows (the gateway still forwards over HTTP). Then, on his instruction: deleted rows D-017 and D-020 and the two answered Open rows (gateway build-vs-buy; gRPC scope), removed D-024's supersede clause and D-013's Redis clause and amend note, set D-031's Status to say gRPC covers the gateway hop (D-013) in the words he gave, changed the login step in the D-010 detail to gRPC, and repointed the five remaining D-017/D-020 mentions in the D-010 detail and `architecture.md` so nothing cites a deleted row. | a few things for the API gateway • ship the containerised API gateway in this PR instead of the frontend PR • a few minor docs updates, the main one - for the switch from REST to gRPC for internal calls, I think we should update D-013 in place instead of appending a new decision record. imo we only append a new record when the original decision was committed on main. since this PR is where the decisions are being defined, we can jst update it • auth bug which you can attempt to replicate and fix • the rest are all compacting and updating any outdated comments | Read all 176 inline comments on PR #4 through `gh api`. **Did not write D-013's new wording, or any other decision row.** The reviewer asked explicitly that "the new wording and the Rationale cells need to be ours, not the AI's", and the same applies to collapsing D-017/D-020/D-024, updating D-025(b), deleting the two answered Open rows, and D-010-detail / `architecture.md`'s LEAK edge; all are handed back. Also left the gRPC line in `api-gateway/AGENTS.md` and root `AGENTS.md`:123 (reviewer: "needs the team"). **Reproduced every bug before fixing it:** each new test was run against the original file and failed (no-exp token accepted; 10 JWKS fetches for 10 calls while failing; no-`kid` misreported; logout 401/500/502 and refresh 401 left the cookie; seven mistyped API paths answered 200 with the SPA), then passed with the fix. The logout fix takes the reviewer's gateway-side option (clear the cookie whatever logout returns); the session stays live server-side until it expires, which refreshing before logout in PR #6 would close. Confirmed the reviewer's finding that deleting the claim-header strip does not fail `TestClientCannotEscalateViaHeader` (it fails `TestUnauthenticatedRequestGetsNoClaimHeaders` and `TestPassthroughPreservesAuthorization`) and corrected `api-gateway/AGENTS.md` to name them. No dependency added. Verified: `go build`, `go vet` clean, `gofmt` clean on LF-normalised content, `go test ./... -count=1` green; `docker build -f api-gateway/Dockerfile .` succeeds on this branch alone (29.6 MB), and the container answered `/healthz` 200, `/api/suppliers` 401, `/api/nope` 404 JSON, and an unreachable logout 502 **with** the expiring `Set-Cookie`; `docker compose config` validates. Not run against a real user-service. Ran no git write and no `gh` write — nothing committed, pushed or merged | Used to debug and make changes according to PR feedback, updated relevant documents, comments and files |
