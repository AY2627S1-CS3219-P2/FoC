# AI Usage Log

Required by CS3219 Appendix 2: _"Maintain a log, `/ai/usage-log.md`, in the
repository with timestamps, prompts, and usage scenarios."_ This log is shared —
every team member appends to it, and so does every AI tool that changes files.

The consolidated AI Use Summary required as the README's last section is
generated from this log, so keep entries accurate rather than tidy. What may and
may not be delegated to an AI tool is defined in **§1 of the root `AGENTS.md`**
— that is the single copy; do not restate it here.

## How to add an entry

Append one row per working session to the table under [Entries](#entries),
newest at the bottom. That is the only table in this file.

| Column        | What goes in it                                                                                                      |
| ------------- | -------------------------------------------------------------------------------------------------------------------- |
| Date/Time     | ISO 8601 local, e.g. `2026-09-15T18:40`                                                                              |
| Member        | The human accountable. An agent fills this from `git config user.name`                                               |
| Tool / model  | e.g. `Claude Code (Opus 5)`, `Codex (GPT-5)`                                                                         |
| Mode          | `generate`, `refactor`, `debug`, `explain`, `docs`, or `test`                                                        |
| Scope         | Files or areas touched, and what changed                                                                             |
| Prompt        | The prompt as actually typed                                                                                         |
| Key responses | What the tool produced or concluded in reply — the decision it made, the approach it took, or what it declined to do |
| Human review  | What you checked and verified — `PENDING` until a human has                                                          |

**Prompts.** Record the prompt as actually typed, not a cleaned-up version — the
policy asks for "the exact prompts", and a reconstruction that reads better than
the original is itself a disclosure problem.

## File-header attribution

Every file an AI tool created, or whose lines it changed more than half of in
one session, carries this block at the top in that file's comment syntax:

```
// AI Assistance Disclosure:
// Tool: <product> (model: <model>), date: <YYYY-MM-DD>
// Scope: <what it generated or changed in this file>
// Author review: PENDING — <reviewer to complete>
```

Below that bar, mark the individual blocks inline instead:
`// AI-generated (edited by <name>).` When unsure which applies, mark inline —
over-disclosing is never the violation.

A human replaces `PENDING` with their name and what they actually checked.
Appendix 2 makes each member "fully responsible for understanding and validating
any AI-assisted code", and every member "remains accountable for understanding
the entire codebase". **Do not sign off on a file you have not read.** An agent
never writes a name into that field, and never removes an existing header.

## Entries

<!-- prettier-ignore -->
| Date/Time | Member | Tool / model | Mode | Scope | Prompt | Key responses | Human review |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-09-15T18:40 | twhjames | Claude Code (Opus 5) | generate | Root + per-service `AGENTS.md`/`CLAUDE.md` and this log — agent-instruction scaffolding, no application code | we will be using claude and codex, so we need both claude.md and agents.md - do we need to have these 2 in each service folder and root folder? what content goes to which? context for our product: Go for backend services, frontend language and framework TBC, OpenAPI for API contracts and SwaggerUI for API testing and communicating specs, Docker for containerisation, GitHub Actions for CI/CD, AWS for cloud deployment plus IaC (suggest one apt for us), and 5 developers with one owning each service plus a frontend developer. we also want proper unit, integration and end-to-end testing, no code smells, and to avoid control, global and temporal coupling and other anti-patterns. | Confirmed from the Claude Code docs that Claude Code reads only `CLAUDE.md` while Codex reads `AGENTS.md`, so both filenames are needed at the root and in each service folder. Proposed a single source of truth: `AGENTS.md` carries all content and every `CLAUDE.md` is a three-line `@AGENTS.md` import, so the two tools cannot drift. Root holds what is repo-wide — stack, folder ownership, the port and env-var allocation, service-independence and coupling rules, code quality, the three test tiers, API-contract and git conventions, and the Appendix 2 boundary. Each service file holds only its own bounded context, boundaries, package layout, local development and gotchas, and references the root rather than repeating it. Root files load at session start while a service pair loads on demand, so work on one service does not pull in the other four. Separately recommended spec-first OpenAPI with `oapi-codegen` and generated code committed, Terraform as the IaC tool once AWS work starts, and splitting container work by ownership — each service's Dockerfile in that service's own PR, the shared Compose baseline in its own. | Read all six AGENTS.md; confirmed the @AGENTS.md import works in Claude Code and that Codex reads AGENTS.md. Checked usage-log.md carries the timestamp, prompt, key-response and scenario fields Appendix 2 asks for. Verified no application code written as well. |
| 2026-09-18T11:28 | Lim-ZY | Codex (GPT-5) | generate | `user-service/Dockerfile`, root `compose.yaml`, `user-service/.dockerignore` — containerized user-service API with PostgreSQL 16 and local build exclusions | Great. Go ahead and implement those now. Then use the $present-changes-visually skill to show the changes you made. Do not commit anything. | Added a multi-stage Go Dockerfile, PostgreSQL 16/user-service Compose services with health-gated startup and persistent database volume, and a Docker build context exclusion file. Generated a self-contained visual diff page; did not commit changes. | COMPLETE |
| 2026-09-18T11:43 | Lim-ZY | Codex (GPT-5) | docs | `user-service/_temp/user-service-scaffold-plan.md` — planning-only scaffold checklist based on the D1 backlog and recorded service instructions | Now based on the following product backlog, use the $iterative-tdd skill to scaffold the user service. You should only touch the user-service subdirectory, but you may read through the whole repository for context first. | Inspected the backlog PDF and repository guidance, distinguished recorded requirements from unresolved API/schema/security decisions, and wrote a scaffold-only checklist. No application code was changed; awaiting human plan approval. | COMPLETE |
| 2026-09-18T11:52 | Lim-ZY | Codex (GPT-5) | generate | `user-service/go.mod`, `user-service/internal/config/config.go`, `user-service/internal/config/config_test.go`, and the iteration plan/diff — initial module and environment configuration scaffold | Great the checklist looks good. Go ahead and proceed to implementing. | Added the module metadata and tested configuration loader for PORT, USER_DB_URL, and the recorded JWT setting names. gofmt passed; go test and go vet were attempted but the installed Go runtime could not recognize its own standard-library packages. Generated a visual diff; did not commit. | COMPLETE |
| 2026-09-18T12:15 | Lim-ZY | Codex (GPT-5) | generate | `user-service/go.mod`, `user-service/go.sum`, and `user-service/internal/user/{model.go,errors.go,repository.go,model_test.go}` — user model and repository contract increment | Great begin implementing | Added the recorded user model, account enums, repository interface, sentinel errors, and contract-focused tests. Added `github.com/google/uuid`; `gofmt`, `go test ./...`, and `go vet ./...` passed after dependency download. Generated a visual diff and did not commit. | COMPLETE |
| 2026-09-18T12:20 | Lim-ZY | Codex (GPT-5) | generate | `user-service/migrations/000001_create_users.{up,down}.sql` — reversible PostgreSQL schema migration | Great proceed with the next iteration. | Added the recorded `users` table, account-role/status enums, uniqueness and nullability constraints, and defaults. `gofmt`, `go test ./...`, and `go vet ./...` passed; the migration itself was not applied because no PostgreSQL/testcontainer environment was available. Generated a visual diff and did not commit. | COMPLETE |
| 2026-09-18T12:30 | Lim-ZY | Codex (GPT-5) | generate | `user-service/internal/repository/postgres.go`, `../user-service/internal/repository/user_postgres_test.go`, and module dependencies — PostgreSQL repository adapter increment | Great proceed with the next iteration. | Added context-aware pgx CRUD operations, Go-side UUID/SGT timestamp/default preparation, row mapping, and specific duplicate/not-found error mapping. Preserved the human-updated duplicate-email/username sentinels. `gofmt`, `go test ./...`, and `go vet ./...` passed after downloading pgx dependencies. Generated a visual diff and did not commit. | COMPLETE |
| 2026-09-18T12:40 | Lim-ZY | Codex (GPT-5) | generate | `user-service/cmd/api/main.go`, `../user-service/internal/repository/migrations.go`, and migration-runner dependencies — PostgreSQL startup wiring increment | Great proceed with the next iteration. | Added service-owned migration execution and a context-aware pgx pool with startup ping through the existing config boundary. `gofmt`, `go vet ./...`, and `go test ./...` passed after downloading migration dependencies. No live database was available, so startup was not exercised against PostgreSQL. Generated a visual diff and did not commit. | COMPLETE |
| 2026-09-18T16:55 | Lim-ZY | Codex (GPT-5) | generate | `user-service/internal/user/{auth.go,auth_test.go,errors.go}` — domain authentication service boundary with injected JWT token issuance | Great, now proceed with implementation. | Added bcrypt credential verification, generic invalid-credential handling, suspended-account rejection, and an injected JWT access/refresh token issuer. Added table-driven unit tests. `go test ./...` and `go vet ./...` passed with temporary writable Go caches; no commit was created. | COMPLETE |
| 2026-09-18T17:20 | Lim-ZY | Codex (GPT-5) | generate | `user-service/internal/httpapi/{router.go,router_test.go}` and `user-service/go.mod`/`go.sum` — Chi HTTP route and handler scaffold | Great, now proceed with the next iteration. | Added Chi route registration for health, registration, login, profile, profile-update, and account-status paths; implemented tested health/login transport behavior and explicit placeholders for remaining domain operations. Added `github.com/go-chi/chi/v5` because the recorded stack requires Chi. `go test ./...` and `go vet ./...` passed with temporary writable caches; no commit was created. | COMPLETE |
| 2026-09-18T18:05 | Lim-ZY | Codex (GPT-5) | generate | `user-service/internal/httpapi/{auth.go,auth_test.go}` — injected JWT verification middleware and session invalidation boundary | Great, now proceed with the next iteration using the $iterative-tdd skill. | Added Bearer-token middleware, verified-principal request context, injected `TokenVerifier`, and `SessionInvalidator` interfaces. Added tests for missing, malformed, invalid, and valid authentication. `go test ./...` and `go vet ./...` passed with temporary writable caches; no commit was created. | COMPLETE |
| 2026-09-19T10:30 | Lim-ZY | Codex (GPT-5) | generate | `user-service/internal/user/{model.go,repository.go,errors.go,auth_test.go,session_test.go}` — recorded session model and repository interfaces | I have specified the required behaviour in `AGENTS.md`. Update the plan in `user-pi-auth-plan.md` as required, then proceeding with the next iteration. | Refined the authentication plan into persistence, JWT, session, endpoint, Redis, and wiring increments. Added `tokens_valid_after`, the `Session` model, session errors, the account-status repository method, and the five session repository operations with focused tests. `go test ./...` and `go vet ./...` passed using temporary writable caches; no commit was created. | COMPLETE |
| 2026-09-19T11:55 | Lim-ZY | Codex (GPT-5) | generate | `user-service/migrations/000002_add_sessions_and_token_validity.{up,down}.sql` — recorded token-validity and refresh-session schema migration | Great, now proceed with the next iteration. | Added the reversible migration for `users.tokens_valid_after` and `sessions`, including the recorded keys, nullability, unique token hash, foreign key, and cascade behavior. `go test ./...` and `go vet ./...` passed; no live PostgreSQL/testcontainer environment was available to apply the migration. No commit was created. | COMPLETE |
| 2026-09-19T13:10 | Lim-ZY | Codex (GPT-5) | generate | `user-service/internal/repository/{session_postgres.go,session_postgres_test.go,contract_test.go,postgres.go}` — PostgreSQL session repository and user-schema compatibility mapping | Great, now proceed with the next iteration. | Added session persistence with row-locked lookup, transactional refresh rotation, revocation methods, and session row/error tests. Updated the user adapter for `tokens_valid_after` and account-status updates. Respected the existing `UserRepository` naming. `go test ./...` and `go vet ./...` passed with temporary writable caches; no commit was created. | COMPLETE |
| 2026-09-19T15:10 | Lim-ZY | Codex (GPT-5) | generate | `user-service/internal/auth/{jwt.go,jwt_test.go}`, `user-service/internal/config/{config.go,config_test.go}`, and `user-service/go.mod`/`go.sum` — RS256 JWT signing, verification, key rotation, JWKS, and key configuration increment | Great, now proceed with the next iteration. | Added the recorded RS256 JWT boundary with issuer/audience/type/role claims, five-second clock skew, active and retired key IDs, signature verification, and deterministic JWKS output. Replaced the obsolete JWT secret configuration with private/public key settings. Added `github.com/golang-jwt/jwt/v5` because the standard library does not provide JWT/JWKS primitives and the repository prohibits home-rolled cryptography. `go test ./...` and `go vet ./...` passed with temporary writable caches; no commit was created. | COMPLETE |
| 2026-09-19T15:35 | Lim-ZY | Codex (GPT-5) | generate | `user-service/internal/user/{auth.go,session.go,auth_test.go,session_service_test.go}` — login and refresh session application services | Great, now proceed with the next iteration. | Added login session creation with SHA-256 refresh-token hashing, refresh rotation, row-repository error handling, replay detection, compromised-user session revocation, account-status/token-validity checks, and focused unit tests. `go test ./...` and `go vet ./...` passed with temporary writable caches; no commit was created. | COMPLETE |
| 2026-09-19T16:00 | Lim-ZY | Codex (GPT-5) | generate | `user-service/internal/user/{account.go,account_test.go,auth_test.go}` — registration, profile update, and account-status application operations | Great, now proceed with the next iteration. | Added bcrypt-backed registration, mutable profile updates, validated ACTIVE/SUSPENDED transitions, repository error propagation, and focused unit tests. Deferred broker event publication because its payload and interface contract are not recorded. `go test ./...` and `go vet ./...` passed with temporary writable caches; no commit was created. | COMPLETE |
| 2026-09-19T16:20 | Lim-ZY | Codex (GPT-5) | generate | `user-service/internal/auth/{jwt.go,jwt_test.go}` — concrete refresh-token verifier adapter | Great, now proceed with the next iteration. | Implemented `VerifyRefresh` on the JWT adapter, mapping verified refresh claims into the domain boundary, rejecting access tokens, and honoring context cancellation. `go test ./...` and `go vet ./...` passed with temporary writable caches; no commit was created. | COMPLETE |
| 2026-09-19T16:40 | Lim-ZY | Codex (GPT-5) | generate | `user-service/internal/auth/{jwt.go,jwt_test.go}` — concrete JWT token issuer integration | Great, now proceed with the next iteration. | Implemented the existing domain `TokenIssuer` interface with recorded 15-minute access and 7-day refresh defaults, including refresh `jti` and expiry metadata for session persistence. Added focused integration tests. `go test ./...` and `go vet ./...` passed with temporary writable caches; no commit was created. | COMPLETE |
| 2026-09-19T19:20 | Lim-ZY | Codex (GPT-5) | generate | `user-service/internal/user/{invalidation.go,invalidation_test.go}` — Redis-first logout invalidation boundary | Great. I have fixed the logout ordering. You may check the `AGENTS.md` file again and proceed with the next iteration. | Added Redis-first logout invalidation, exact remaining access-token TTL calculation, hashed refresh-session revocation, Redis failure short-circuiting, and focused ordering/error tests. Deferred suspension invalidation because the repository contract does not accept the exact shared `tokens_valid_after` timestamp required by the recorded behavior. `go test ./...` and `go vet ./...` passed with temporary writable caches; no commit was created. | COMPLETE |
| 2026-09-19T21:00 | Lim-ZY | Codex (GPT-5) | generate | `user-service/internal/user/{repository.go,account.go,auth_test.go,account_test.go}` and `user-service/internal/repository/user_postgres.go` — caller-provided suspension timestamp contract | Great, now proceed with the next iteration. | Refactored `UpdateAccountStatusByID` so callers provide the suspension timestamp, passed it unchanged through the PostgreSQL adapter, required it for suspension transitions, and added focused contract/application tests. `go test ./...` and `go vet ./...` passed with temporary writable caches; no commit was created. | COMPLETE |
| 2026-09-20T09:15 | Lim-ZY | Codex (GPT-5) | generate | `user-service/internal/httpapi/{handlers.go,dto.go,router.go,router_test.go,refresh_test.go}` — recorded refresh HTTP endpoint | Read through the `AGENTS.md` file again, then continue with the next iteration | Implemented the OpenAPI-recorded unauthenticated refresh endpoint, camelCase request/response DTOs, invalid/expired and compromised-session 401 mappings, and focused HTTP tests. `go test ./...` and `go vet ./...` passed with temporary writable caches; no commit was created. | COMPLETE |
| 2026-09-20T10:00 | Lim-ZY | Codex (GPT-5) | generate | `user-service/internal/httpapi/{handlers.go,dto.go,router_test.go}` — login OpenAPI response alignment | Note that the login handler should follow the openapi contract which uses camelCase. Add that to the checklist in the markdown file, then proceed with the next iteration. | Replaced the legacy snake_case login token response with the recorded camelCase `AuthResponse` shape and updated the focused handler test. `go test ./...` and `go vet ./...` passed with temporary writable caches; no commit was created. | COMPLETE |
| 2026-09-20T10:20 | Lim-ZY | Codex (GPT-5) | generate | `user-service/internal/repository/{blocklist_redis.go,blocklist_redis_test.go}` and `user-service/go.mod`/`go.sum` — Redis access-token blocklist adapter | Great, now proceed with the next iteration. | Added the recorded `go-redis/v9` adapter, `jti:<uuid>` key format, exact TTL propagation, input/error handling, and focused unit tests. Added `github.com/redis/go-redis/v9` because the recorded Redis integration requires an approved Redis protocol client, which the standard library does not provide. `go mod tidy`, `go test ./...`, and `go vet ./...` passed with temporary writable caches; no commit was created. | COMPLETE |
| 2026-09-20T11:05 | Lim-ZY | Codex (GPT-5) | generate | `user-service/internal/user/invalidation.go`, `user-service/internal/auth/{jwt.go,jwt_test.go}`, and `user-service/internal/httpapi/{handlers.go,dto.go,router.go,router_test.go,logout_test.go}` — recorded logout HTTP endpoint and access-token verification boundary | Great, now proceed with the next iteration. | Added the `/api/v1/users/logout` handler, camelCase refresh-token DTO, concrete RS256 access-claim adapter, and focused tests for success, authentication failures, malformed input, and invalidation failure. `gofmt`, `go test ./...`, and `go vet ./...` passed with temporary writable caches. `git diff --check` reports only existing trailing whitespace in `user-service/AGENTS.md`. No commit was created. | COMPLETE |
| 2026-09-20T12:00 | Lim-ZY | Codex (GPT-5) | generate | `user-service/internal/httpapi/{handlers.go,router.go,router_test.go,jwks_test.go}` — recorded JWKS HTTP endpoint | Great, now proceed with the next iteration. | Added the unauthenticated `/.well-known/jwks.json` route with an injected public-JWKS provider and JSON response, plus focused success and provider-failure tests. The existing JWT adapter supplies active and retired public keys. `gofmt`, `go test ./...`, and `go vet ./...` passed with temporary writable caches; no commit was created. | COMPLETE |
| 2026-09-20T13:00 | Lim-ZY | Codex (GPT-5) | generate | `user-service/cmd/api/{main.go,main_test.go}`, `internal/config/{config.go,config_test.go}`, `internal/auth/{jwt.go,key_loader.go,key_loader_test.go}`, and `internal/httpapi/{handlers.go,router_test.go}` — recorded dependency startup wiring | Great, now proceed with the next iteration. | Added `JWT_KEYSET_PATH`/Redis configuration, JSON PEM key-set loading with first-key signing and SHA-256-derived key IDs, configured JWT TTL validation, Postgres/Redis/JWT/application-service/router startup wiring, and corrected the HTTP login seam to use the session-persisting login service. `gofmt`, `go test ./...`, and `go vet ./...` passed with temporary writable caches. Startup was not exercised against live PostgreSQL/Redis/private key material. No commit was created. | COMPLETE |
| 2026-09-20T13:20 | Lim-ZY | Codex (GPT-5) | generate | `user-service/.env.example` — recorded service-local development configuration placeholders | You may put the `.env.example` in the `user-service` directory. Then proceed with the next iteration. | Added non-secret placeholders for the recorded port, PostgreSQL, Redis, JSON JWT key-set, and token-lifetime configuration. `go test ./...` and `go vet ./...` passed with temporary writable caches; no commit was created. | COMPLETE |
| 2026-09-20T13:45 | Lim-ZY | Codex (GPT-5) | generate | `user-service/internal/httpapi/{dto.go,handlers.go,router.go,register_test.go}` and `cmd/api/main.go` — recorded registration HTTP endpoint | Great. Proceed with the next iteration. | Implemented unauthenticated registration with the recorded request fields, `201` empty success, duplicate username/email `409`, focused error handling, and account-service startup injection. Broker publication remains deferred because its contract is not recorded. `gofmt`, `go test ./...`, and `go vet ./...` passed with temporary writable caches; no commit was created. | COMPLETE |
| 2026-09-20T14:10 | Lim-ZY | Codex (GPT-5) | generate | `user-service/internal/httpapi/{dto.go,handlers.go,router.go,profile_test.go}` and `cmd/api/{main.go,principal_verifier_test.go}` — recorded authenticated public-profile endpoint | Great. Proceed with the next iteration. | Implemented the OpenAPI-recorded authenticated profile lookup, public response mapping, not-found handling, and a tested edge adapter that maps only verified access JWT claims into the existing HTTP principal boundary. `gofmt`, `go test ./...`, and `go vet ./...` passed with temporary writable caches; no commit was created. | COMPLETE |
| 2026-09-20T14:30 | Lim-ZY | Codex (GPT-5) | generate | `user-service/internal/httpapi/{dto.go,handlers.go,router.go,status_test.go}` and `cmd/api/main.go` — recorded ADMIN-only account-status HTTP endpoint | Great. Proceed with the next iteration. | Implemented authenticated admin-role enforcement, status input validation, caller-generated PostgreSQL timestamp propagation, and focused status endpoint tests. Redis suspension invalidation remains explicitly deferred to the final project iteration. `gofmt`, `go test ./...`, and `go vet ./...` passed with temporary writable caches; no commit was created. | COMPLETE |
| 2026-09-20T14:45 | Lim-ZY | Codex (GPT-5) | generate | `user-service/internal/httpapi/{dto.go,handlers.go,router.go,profile_update_test.go}` and `cmd/api/main.go` — recorded self-or-admin profile update | Great. Proceed with the next iteration. | Implemented recorded self-or-admin authorization, profile update mapping, and focused authorization tests. `gofmt`, `go test ./...`, and `go vet ./...` passed; no commit was created. | COMPLETE |
| 2026-09-20T15:30 | Lim-ZY | Codex (GPT-5) | refactor | `user-service/internal/httpapi/{handlers.go,auth_handler.go,profile_handler.go,system_handler.go,router.go}`; related HTTP tests; `cmd/api/main.go` — recorded handler-group extraction | Great. Proceed with the refactoring | Replaced the monolithic transport handler with recorded AuthHandler, ProfileHandler, and SystemHandler groups, each with only its endpoint-specific dependencies. Updated router wiring, application composition, and HTTP tests. `go test ./...`, `go vet ./...`, and `git diff --check` passed. Generated the split-view visual diff; no commit created. | COMPLETE |
| 2026-09-20T15:15 | Lim-ZY | Codex (GPT-5) | refactor | `user-service/internal/user/session.go` — extracted focused helpers from refresh-token rotation workflow | In `session.go`, the `Refresh()` function is too long. Logically refactor it using helper methods, then use the $present-changes-visually skill to show the changes | Split `Refresh` into dependency, token-claim, session, account, token/session issuance, rotation, and compromise-revocation helpers while preserving the existing workflow and error mappings. `go test ./internal/user`, `go vet ./internal/user`, and `git diff --check` passed; visual diff pending. | COMPLETE |
| 2026-09-21T09:00 | Lim-ZY | Codex (GPT-5) | refactor | `_temp/httpapi-packaging-plan.md`, `user-service/internal/httpapi/handlers/`, forwarding layer, and router — first HTTP API packaging increment | Consider the new plan stated under `Packaging` in `AGENTS.md`. Then create a plan to re-refactor what you did. Use the $iterative-tdd workflow and clarify anything that is not clear before implementing them. Great. Proceed with the first iteration of refactoring | Confirmed the recorded handler-to-routes decision, wrote the approved checklist, then moved Auth, Profile, and System handlers with their DTOs, JSON helper, and JWT middleware into `httpapi/handlers`. Added direct handler regression coverage and forwarding aliases to preserve current routing. `go test ./...`, `go vet ./...`, and `git diff --check` passed. Generated an iteration-local visual diff; no commit created. | COMPLETE |
| 2026-09-21T09:20 | Lim-ZY | Codex (GPT-5) | refactor | `user-service/internal/httpapi/routes/` and `_temp/httpapi-packaging-plan.md` — second HTTP API packaging increment | Great. Proceed with the next iteration | Added `routes.GetRoutes()` to construct grouped handlers from their own dependency configurations and register all recorded routes with unchanged JWT protection. Added focused route-registration coverage. `go test ./...`, `go vet ./...`, and `git diff --check` passed. Generated an iteration-local visual diff; no commit created. | COMPLETE |
| 2026-09-21T09:40 | Lim-ZY | Codex (GPT-5) | refactor | `user-service/internal/httpapi/router/` and `_temp/httpapi-packaging-plan.md` — third HTTP API packaging increment | Great. Proceed with the next iteration | Added `router.Setup()` and its private route-mounting helper, delegating to `routes.GetRoutes()`. Added focused router setup coverage. `go test ./...`, `go vet ./...`, and `git diff --check` passed. Generated an iteration-local visual diff; no commit created. | COMPLETE |
| 2026-09-21T10:00 | Lim-ZY | Codex (GPT-5) | refactor | `user-service/cmd/api/main.go`, HTTP tests, and removed root HTTP compatibility layer — final HTTP API packaging increment | Great. Proceed with the next iteration | Rewired startup and HTTP tests to construct route dependencies and call `router.Setup()`, then removed the temporary aliases and root router. The `handlers`, `routes`, and `router` packages now own their recorded responsibilities. `go test ./...`, `go vet ./...`, and `git diff --check` passed. Generated an iteration-local visual diff; no commit created. | COMPLETE |
| 2026-09-21T11:00 | Lim-ZY | Codex (GPT-5) | docs | `_temp/user-packaging-plan.md` — revised dependency-safe user-domain packaging checklist | Great. Proceed with the next iteration. | Found that moving `auth.go` separately would create an import cycle with `session.go` after `internal/session` depends on the retained user-domain types. Combined the authentication, lifecycle, and invalidation moves into one atomic future increment; no application code changed. | COMPLETE |
| 2026-09-21T11:20 | Lim-ZY | Codex (GPT-5) | refactor | `user-service/internal/session/`, affected JWT/repository/HTTP/startup consumers, and tests — atomic session packaging increment | Great. Proceed with the next iteration. | Moved authentication, login/refresh lifecycle, and Redis-first invalidation into `internal/session`; updated consumers and relocated session tests. Kept `internal/user` for domain models, errors, repositories, and account services. `go test ./...`, `go vet ./...`, and `git diff --check` passed. Generated an iteration-local visual diff; no commit created. | COMPLETE |
| 2026-09-21T12:00 | Lim-ZY | Codex (GPT-5) | docs | `user-service/README.md` — documented startup, API calls, and testing commands based on the current implementation | Based on the codebase so far, add contents to the `README.md` for the user service. It should include these sections: 1. Steps to start the service 2. API Calls as a table (Method, path, auth, remarks) 3. How to test Do not modify any other code besides the `README.md` file | Added the requested README sections, documented the current routes and authentication requirements, and included local dependency/key setup plus unit-test and static-check commands. | COMPLETE |
| 2026-09-21T12:10 | Lim-ZY | Codex (GPT-5) | test | `user-service/internal/httpapi/register_test.go` — updated the successful registration fixture to use a NUS email and added a non-NUS email rejection case | Add a test case for auth_handler to test the current implementation. | Added coverage expecting `400 Bad Request` and the current error message for an email that does not end with `@u.nus.edu`; test execution is currently blocked by compile errors in the user-edited handler implementation. | COMPLETE |
| 2026-09-21T12:20 | Lim-ZY | Codex (GPT-5) | test | `user-service/internal/user/account_test.go` and `user-service/internal/session/auth_test.go` — added case-insensitive email registration and login tests | Add the tests for this case, and test it. | Added tests requiring registration to store a trimmed lowercase email and login to look up a trimmed lowercase email identifier. Both focused tests compile but currently fail because production code passes the original email and identifier unchanged. | COMPLETE |
| 2026-09-21T12:45 | Lim-ZY | Codex (GPT-5) | generate | `user-service/api/openapi.yaml`, `user-service/internal/httpapi/handlers/{profile_handler.go,dto.go}`, `user-service/internal/httpapi/profile_test.go` — role-based profile response visibility | Great. Proceed with the implementation | Added admin-only full profile responses, restricted non-admin responses containing only uid, username, email, and phone number, updated the OpenAPI response documentation, and added focused HTTP tests. `gofmt`, `go test ./...`, `go vet ./...`, and `git diff --check` passed. | COMPLETE |
