// AI Assistance Disclosure:
// Tool: Claude Code (model: Opus 5), date: 2026-09-19
// Scope: Empty test file, part of the scaffold. No tests written.
// Author review: PENDING — <reviewer to complete>

package config_test

// Intentionally empty. Tests for Load belong here — at minimum that a missing
// variable is reported rather than defaulted (root AGENTS.md §7: cover the
// error paths, not just the happy ones).
